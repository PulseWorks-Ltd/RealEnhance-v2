// Stage 1A: quality enhancement
export async function runStage1A(originalPath: string): Promise<string> {
  // TODO: run haze removal / brightness / color / straighten
  // Save to new file and return that path.
  return originalPath;
}
