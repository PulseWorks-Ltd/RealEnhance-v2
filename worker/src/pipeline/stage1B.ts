// Stage 1B: declutter / depersonalize / remove existing furniture
export async function runStage1B(
  stage1APath: string,
  opts: { sceneType: string }
): Promise<string> {
  // TODO: remove clutter, wipe counters, tidy gardens for exteriors, etc.
  // Save new file and return path.
  return stage1APath;
}
